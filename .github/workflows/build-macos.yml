name: Build macOS

on:
  push:
    branches:
      - macos
      - main
    paths:
      - 'src/macOS/**'
      - 'src/Shared/**'
      - '.github/workflows/build-macos.yml'

jobs:
  build:
    runs-on: macos-latest

    strategy:
      matrix:
        arch: [arm64, x64]

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '8.0.x'

    - name: Restore macOS dependencies
      run: dotnet restore src/macOS/ClaudePermissionAssistant.MacApp/ClaudePermissionAssistant.MacApp.csproj -r osx-${{ matrix.arch }}

    - name: Build macOS app
      run: dotnet build src/macOS/ClaudePermissionAssistant.MacApp/ClaudePermissionAssistant.MacApp.csproj --configuration Release -r osx-${{ matrix.arch }} --no-restore

    - name: Publish macOS app
      run: |
        dotnet publish src/macOS/ClaudePermissionAssistant.MacApp/ClaudePermissionAssistant.MacApp.csproj \
          -c Release \
          -r osx-${{ matrix.arch }} \
          --self-contained true \
          -p:PublishSingleFile=true \
          -o publish/osx-${{ matrix.arch }}

    - name: Upload artifact
      uses: actions/upload-artifact@v4
      with:
        name: macos-build-${{ matrix.arch }}
        path: publish/osx-${{ matrix.arch }}/ClaudePermissionAssistant
        retention-days: 90
