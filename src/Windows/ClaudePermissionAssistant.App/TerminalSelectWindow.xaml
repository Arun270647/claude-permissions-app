<Window x:Class="ClaudePermissionAssistant.App.TerminalSelectWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Select Terminal" Height="350" Width="500"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" Text="Select a terminal to monitor:"
                       FontSize="13" FontWeight="Bold" VerticalAlignment="Center"/>
            <Button Grid.Column="1" Content="Refresh" Width="70" Height="28"
                    Click="Refresh_Click"/>
        </Grid>

        <ListBox Name="TerminalListBox" Grid.Row="1" Margin="0,0,0,10"
                 MouseDoubleClick="TerminalListBox_MouseDoubleClick">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Margin="5">
                        <TextBlock Text="{Binding DisplayName}" FontWeight="Bold"/>
                        <TextBlock Text="{Binding DisplayDetails}" FontSize="11" Foreground="Gray"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel" Width="80" Height="30" Margin="0,0,10,0"
                    Click="Cancel_Click"/>
            <Button Name="SelectButton" Content="Select" Width="80" Height="30"
                    Click="Select_Click" IsEnabled="False"
                    Background="#4CAF50" Foreground="White" FontWeight="Bold"/>
        </StackPanel>
    </Grid>
</Window>
